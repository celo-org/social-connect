{
  "name": "@celo/odis-identifiers",
  "author": "cLabs",
  "license": "Apache-2.0",
  "version": "1.0.1",
  "description": "ODIS Identifier Prefixes and functions to hash identifiers",
  "repository": "https://github.com/celo-org/social-connect/tree/main/packages/odis-identifiers",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "yarn run --top-level tsc -b .",
    "clean": "yarn run --top-level tsc -b . --clean",
    "test": "yarn run --top-level jest --runInBand",
    "lint": "yarn run --top-level eslint ."
  },
  "devDependencies": {
    "web3-utils": "4.2.1"
  }
}
